# Candor Design System

**Version 2.2 · April 2026**

Candor is a design system built on a single premise: *good design tells the truth about what works.* Passing a checklist and working for people are different achievements. The system is built around the second one — for the widest range of people, in the widest range of contexts.

The name comes from a direct line through the work. Accessibility standards that produce false passes, safety benchmarks optimized for clearing thresholds rather than catching failures, governance processes that accept input from affected populations without giving them a seat — these are all failures of candor. A design system named Candor is a commitment to close that gap.

---

## Design Principles

### Accessibility is the baseline, not the finish line
WCAG compliance is where we start. Outcome-based validation — real color science, real user impact — is where we go from there. Every token, every component, every contrast decision is validated using OKCA — a contrast algorithm that addresses known issues with WCAG's luminance math while prioritizing WCAG compatibility and coherence with OKLCH, so designers can directly equate their color pickers and tonal ramps with accessibility outcomes.

### Perceptual truth over nominal correctness
Colors are defined in OKLCH — a perceptually uniform color space where equal numeric changes produce equal perceived changes. This isn't an aesthetic preference; it's what makes programmatic accessibility validation reliable.

### Typography as cognitive infrastructure
The typographic system is organized around two human modes: **execution** (task completion, navigation, scanning) and **interpretation** (reading, reflecting, conversing). Font choices aren't decorative — they signal which mode a surface is designed for.

### No hard-coded values, ever
All styling flows from design tokens. Components never reach past their token layer. This isn't a rule for tidiness — it's what makes the system auditable, adjustable, and honest about its own decisions.

---

## Typographic System

Candor uses a four-voice typographic system aligned to cognitive mode and content type.

<table>
  <thead>
    <tr><th>Voice</th><th>Typeface</th><th>Mode</th><th>Primary Use</th></tr>
  </thead>
  <tbody>
    <tr><td>Structural Sans</td><td>Roboto Flex</td><td>Execution</td><td>Navigation, UI scaffolding, data-dense components</td></tr>
    <tr><td>Technical Mono</td><td>Roboto Mono</td><td>Execution</td><td>Code, logs, terminal environments</td></tr>
    <tr><td>Accessibility Anchor</td><td>Atkinson Hyperlegible</td><td>Execution</td><td>Critical UI, labels, high-contrast environments</td></tr>
    <tr><td>Human-Centered</td><td>Noto Sans / Noto Serif</td><td>Interpretation</td><td>Long-form reading, conversational UI, multilingual content</td></tr>
  </tbody>
</table>

**Scale:** Major Third ratio (1.25×) from a 1rem (16px) base.
**Grid:** 8px spacing unit throughout.

---

## Color Philosophy

The palette draws from personal brand anchors — deep navy (`#082840`), warm burgundy (`#5F2B48`), and azure (`#1493FB`) — extended into a full OKLCH token system. Two-tier architecture keeps the system honest: primitives are raw tonal ramps, semantic tokens map those ramps to roles. Components reference semantic tokens only — never primitives directly.

Every semantic color pair has a documented contrast validation against its intended background. The validation methodology is in the next section.

---

## Contrast Methods

Regulatory exposure, outcome validation, and research comparison are different questions — no single algorithm answers all three. WCAG2 answers the first. OKCA answers the second, and is the primary validation method: it addresses known issues with WCAG's luminance math, maintains WCAG compatibility, and is coherent with OKLCH, so the same numeric space used to author colors is used to validate them. The others provide research context.

WCAG 2.x AA is the floor. OKCA raises the bar at small sizes, where WCAG's math compresses in ways that produce passing scores for combinations that don't read. At 14px — the system's readable-text minimum — OKCA requires 9.5 for regular-weight text and 6.5 for bold, roughly double what WCAG requires at 16px. Use-case context adjusts further: recognition tasks (short labels, redundantly-coded status text) qualify for reduced thresholds because the perceptual task is different from sequential reading. The tier system is in `docs/VISUAL-DESIGN.md`.

<table>
  <thead>
    <tr><th>Method</th><th>Standard</th><th>Notes</th></tr>
  </thead>
  <tbody>
    <tr><td>WCAG2</td><td>WCAG 2.x</td><td>Regulatory floor; luminance ratio</td></tr>
    <tr><td>APCA</td><td>WCAG 3 (draft)</td><td>Perceptual contrast; polarity-aware</td></tr>
    <tr><td>BPCA</td><td>Bridge-PCA</td><td>APCA-compatible; backwards-bridge</td></tr>
    <tr><td>OKCA / PCA</td><td>cpqi 0.1.0</td><td>Primary validation method; WCAG-compatible, zero false-pass guarantee</td></tr>
    <tr><td>DeltaE</td><td>Perceptual distance</td><td>Supplementary; different scale</td></tr>
  </tbody>
</table>

---

## What's Here

Use the sidebar to explore components by category:

- **Design Tokens** — Color palette (light and dark), spacing scale, typography system, icon set
- **Typography** — Heading hierarchy, body text, accessible text (Atkinson Hyperlegible), article layout with `:visited` double-underline
- **Components/Accordion** — Native `<details>`/`<summary>` with variant system (default, subtle, quiet)
- **Components/Alert** — Inline status messaging with info, success, warning, error variants
- **Components/Badge** — Status and label badges with semantic color variants
- **Components/Breadcrumb** — Navigation trail with `aria-current="page"` on current location
- **Components/Button** — Primary, secondary, tertiary, ghost variants; size scale
- **Components/Card** — Surface container with optional header, body, and footer slots
- **Components/Chip** — Compact filter and selection tokens
- **Components/Disclosure** — Single show/hide toggle following the APG Disclosure pattern
- **Components/Drawer** — Slide-in panel anchored to viewport edge; left, right, bottom orientations
- **Components/Menu** — Dropdown action menu
- **Components/Modal** — Dialog with size variants, signal-driven open/close
- **Components/Navigation** — Horizontal and vertical orientations with active state and badges
- **Components/Pagination** — Page navigation with ellipsis collapse and `aria-current="page"`
- **Components/Progress** — Determinate and indeterminate progress indicators
- **Components/Stat** — Large value display with unit, label, and semantic color
- **Components/Table** — Full and compact variants with zebra striping
- **Components/Tabs** — Keyboard-navigable tab panel with ARIA roles
- **Components/Toast** — Info, success, warning, error notifications with dismiss
- **Components/Toolbar** — Grouped action bar with separator, toggle button, and vertical orientation support
- **Components/Tooltip** — Hover/focus overlay with `aria-hidden` by design
- **Form/ChatInput** — Textarea with send action and disclaimer slot
- **Form/Checkbox** — Single and group patterns
- **Form/Combobox** — Filterable dropdown with keyboard navigation
- **Form/Input** — Text, number, multiline; label, hint, error states
- **Form/Listbox** — Single and multi-select list
- **Form/Radio** — Group patterns with fieldset/legend
- **Form/Select** — Native select with design system styling
- **Form/Slider** — Range input with fill gradient and OKLCH lightness axis support
- **Form/Switch** — Toggle with two-way bindable state
- **Examples** — Composed stories showing components working together: Login, Registration, Settings, Data table with filtering, Document and image editors

Every component story includes controls for all inputs and demonstrates all variants.

---

*Candor is a personal design system by John Z. Rioflorido — design technologist and ethical systems designer, working at the intersection of cartography, accessibility, and AI safety.*
